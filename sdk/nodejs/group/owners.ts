// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as inputs from "../types/input";
import * as outputs from "../types/output";
import * as utilities from "../utilities";

/**
 * Manage owners for a group in bulk. Uses the groupId as the resource ID. The resource is authoritative: any owners on the group not declared in configuration will be removed.
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as okta from "@pulumi/okta";
 *
 * const owner1 = new okta.user.User("owner1", {
 *     firstName: "Alice",
 *     lastName: "Owner",
 *     login: "alice-owner@example.com",
 *     email: "alice-owner@example.com",
 * });
 * const owner2 = new okta.user.User("owner2", {
 *     firstName: "Bob",
 *     lastName: "Owner",
 *     login: "bob-owner@example.com",
 *     email: "bob-owner@example.com",
 * });
 * const grp = new okta.group.Group("grp", {name: "demo-group"});
 * const owners = new okta.group.Owners("owners", {
 *     groupId: grp.id,
 *     owners: [
 *         {
 *             type: "USER",
 *             id: owner1.id,
 *         },
 *         {
 *             type: "USER",
 *             id: owner2.id,
 *         },
 *     ],
 * });
 * ```
 *
 * ## Import
 *
 * ```sh
 * $ pulumi import okta:group/owners:Owners example <group_id>
 * ```
 */
export class Owners extends pulumi.CustomResource {
    /**
     * Get an existing Owners resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: OwnersState, opts?: pulumi.CustomResourceOptions): Owners {
        return new Owners(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'okta:group/owners:Owners';

    /**
     * Returns true if the given object is an instance of Owners.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is Owners {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === Owners.__pulumiType;
    }

    /**
     * The ID of the Okta group.
     */
    declare public readonly groupId: pulumi.Output<string>;
    /**
     * Desired owners for the group.
     */
    declare public readonly owners: pulumi.Output<outputs.group.OwnersOwner[] | undefined>;

    /**
     * Create a Owners resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: OwnersArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: OwnersArgs | OwnersState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as OwnersState | undefined;
            resourceInputs["groupId"] = state?.groupId;
            resourceInputs["owners"] = state?.owners;
        } else {
            const args = argsOrState as OwnersArgs | undefined;
            if (args?.groupId === undefined && !opts.urn) {
                throw new Error("Missing required property 'groupId'");
            }
            resourceInputs["groupId"] = args?.groupId;
            resourceInputs["owners"] = args?.owners;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(Owners.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering Owners resources.
 */
export interface OwnersState {
    /**
     * The ID of the Okta group.
     */
    groupId?: pulumi.Input<string>;
    /**
     * Desired owners for the group.
     */
    owners?: pulumi.Input<pulumi.Input<inputs.group.OwnersOwner>[]>;
}

/**
 * The set of arguments for constructing a Owners resource.
 */
export interface OwnersArgs {
    /**
     * The ID of the Okta group.
     */
    groupId: pulumi.Input<string>;
    /**
     * Desired owners for the group.
     */
    owners?: pulumi.Input<pulumi.Input<inputs.group.OwnersOwner>[]>;
}
