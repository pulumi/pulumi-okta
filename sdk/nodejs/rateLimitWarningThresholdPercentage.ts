// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as utilities from "./utilities";

/**
 * Rate limit warning threshold percentage provides operations to manage threshold for warning notifications when the API's rate limit is exceeded.
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as okta from "@pulumi/okta";
 *
 * const example = new okta.RateLimitWarningThresholdPercentage("example", {warningThreshold: 90});
 * ```
 *
 * ## Import
 *
 * ```sh
 * $ pulumi import okta:index/rateLimitWarningThresholdPercentage:RateLimitWarningThresholdPercentage example .
 * ```
 */
export class RateLimitWarningThresholdPercentage extends pulumi.CustomResource {
    /**
     * Get an existing RateLimitWarningThresholdPercentage resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: RateLimitWarningThresholdPercentageState, opts?: pulumi.CustomResourceOptions): RateLimitWarningThresholdPercentage {
        return new RateLimitWarningThresholdPercentage(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'okta:index/rateLimitWarningThresholdPercentage:RateLimitWarningThresholdPercentage';

    /**
     * Returns true if the given object is an instance of RateLimitWarningThresholdPercentage.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is RateLimitWarningThresholdPercentage {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === RateLimitWarningThresholdPercentage.__pulumiType;
    }

    /**
     * The threshold value (percentage) of a rate limit that, when exceeded, triggers a warning notification. By default, this value is 90 for Workforce orgs and 60 for CIAM orgs.
     */
    declare public readonly warningThreshold: pulumi.Output<number>;

    /**
     * Create a RateLimitWarningThresholdPercentage resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args: RateLimitWarningThresholdPercentageArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: RateLimitWarningThresholdPercentageArgs | RateLimitWarningThresholdPercentageState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as RateLimitWarningThresholdPercentageState | undefined;
            resourceInputs["warningThreshold"] = state?.warningThreshold;
        } else {
            const args = argsOrState as RateLimitWarningThresholdPercentageArgs | undefined;
            if (args?.warningThreshold === undefined && !opts.urn) {
                throw new Error("Missing required property 'warningThreshold'");
            }
            resourceInputs["warningThreshold"] = args?.warningThreshold;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(RateLimitWarningThresholdPercentage.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering RateLimitWarningThresholdPercentage resources.
 */
export interface RateLimitWarningThresholdPercentageState {
    /**
     * The threshold value (percentage) of a rate limit that, when exceeded, triggers a warning notification. By default, this value is 90 for Workforce orgs and 60 for CIAM orgs.
     */
    warningThreshold?: pulumi.Input<number>;
}

/**
 * The set of arguments for constructing a RateLimitWarningThresholdPercentage resource.
 */
export interface RateLimitWarningThresholdPercentageArgs {
    /**
     * The threshold value (percentage) of a rate limit that, when exceeded, triggers a warning notification. By default, this value is 90 for Workforce orgs and 60 for CIAM orgs.
     */
    warningThreshold: pulumi.Input<number>;
}
