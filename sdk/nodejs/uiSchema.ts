// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***

import * as pulumi from "@pulumi/pulumi";
import * as inputs from "./types/input";
import * as outputs from "./types/output";
import * as utilities from "./utilities";

/**
 * Manages UI Schema. This resource allows you to create and configure an Okta [UI Schema](https://developer.okta.com/docs/api/openapi/okta-management/management/tag/UISchema/#tag/UISchema) for form layouts and controls.
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as okta from "@pulumi/okta";
 *
 * const example = new okta.UiSchema("example", {uiSchema: {
 *     buttonLabel: "submit",
 *     type: "Group",
 *     elements: [{
 *         scope: "#/properties/lastName",
 *     }],
 * }});
 * ```
 *
 * ## Import
 *
 * ```sh
 * $ pulumi import okta:index/uiSchema:UiSchema example <ui_schema_id>
 * ```
 */
export class UiSchema extends pulumi.CustomResource {
    /**
     * Get an existing UiSchema resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    public static get(name: string, id: pulumi.Input<pulumi.ID>, state?: UiSchemaState, opts?: pulumi.CustomResourceOptions): UiSchema {
        return new UiSchema(name, <any>state, { ...opts, id: id });
    }

    /** @internal */
    public static readonly __pulumiType = 'okta:index/uiSchema:UiSchema';

    /**
     * Returns true if the given object is an instance of UiSchema.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    public static isInstance(obj: any): obj is UiSchema {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === UiSchema.__pulumiType;
    }

    /**
     * Properties of the UI schema.
     */
    declare public readonly uiSchema: pulumi.Output<outputs.UiSchemaUiSchema | undefined>;

    /**
     * Create a UiSchema resource with the given unique name, arguments, and options.
     *
     * @param name The _unique_ name of the resource.
     * @param args The arguments to use to populate this resource's properties.
     * @param opts A bag of options that control this resource's behavior.
     */
    constructor(name: string, args?: UiSchemaArgs, opts?: pulumi.CustomResourceOptions)
    constructor(name: string, argsOrState?: UiSchemaArgs | UiSchemaState, opts?: pulumi.CustomResourceOptions) {
        let resourceInputs: pulumi.Inputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState as UiSchemaState | undefined;
            resourceInputs["uiSchema"] = state?.uiSchema;
        } else {
            const args = argsOrState as UiSchemaArgs | undefined;
            resourceInputs["uiSchema"] = args?.uiSchema;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(UiSchema.__pulumiType, name, resourceInputs, opts);
    }
}

/**
 * Input properties used for looking up and filtering UiSchema resources.
 */
export interface UiSchemaState {
    /**
     * Properties of the UI schema.
     */
    uiSchema?: pulumi.Input<inputs.UiSchemaUiSchema>;
}

/**
 * The set of arguments for constructing a UiSchema resource.
 */
export interface UiSchemaArgs {
    /**
     * Properties of the UI schema.
     */
    uiSchema?: pulumi.Input<inputs.UiSchemaUiSchema>;
}
